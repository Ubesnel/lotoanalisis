<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_users_form_inherit_sorteos" model="ir.ui.view">
        <field name="name">res.users.form.inherit.sorteos</field>
        <field name="model">res.users</field>
        <field name="inherit_id" ref="base.view_users_form"/>
        <field name="arch" type="xml">
            <xpath expr="//notebook" position="inside">
                <page string="Sorteos" name="lottery_sorteos"
                      groups="lottery_base.group_lottery_manager">
                    <field name="sorteo_ids" widget="many2many_tags"/>
                </page>
            </xpath>
        </field>
    </record>
</odoo>
